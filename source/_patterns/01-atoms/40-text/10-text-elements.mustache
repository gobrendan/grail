<p>
  This is a paragraph.
</p>

<blockquote>
	<p>
    This is a blockquote.
	</p>
</blockquote>

<p>
  <a href="#">This is a text link</a>.
</p>

<p>
  The <b>b element</b> is stylistically different text from normal text.
</p>

<p>
  The <strong>strong element</strong> is stylistically different text from normal text.
</p>

<p>
  The <i>i element</i> is stylistically different text from normal text.
</p>

<p>
  The <em>em element</em> is stylistically different text from normal text.
</p>

<p>
  The <u>u element</u> is stylistically different text from normal text.
</p>

<p>
  These are examples of the <del>del element</del> (deleted) and <ins>ins element</ins> (inserted).
</p>

<p>
  This is an example of the <s>s element</s> (strikethrough). 
</p>

<p>
  Here's a superscript<sup>®</sup> example.
</p>

<p>
  A subscript example would look like H<sub>2</sub>O.
</p>

<p>
  <small>This is how small text should look.</small>
</p>

<p>
  An abbreviation would look like <abbr title="HyperText Markup Language">HTML</abbr>.
</p>

<p>
  Keyboard input would look like <kbd>Cmd</kbd>.
</p>

<p>
  <q cite="https://google.com">A short, inline quotation look like this</q>.
</p>

<p>
  <cite>A general citation looks like this.</cite>
</p>

<p>
  The <dfn>dfn element</dfn> indicates a definition.
</p>

<p>
  The <mark>mark element</mark> indicates a highlight.
</p>

<p>
  <code>This is what inline code looks like.</code>
</p>

<p>
  <var>This is a variable element</var>.
</p>

<p>
  <samp>This is sample output from a computer program</samp>.
</p>
