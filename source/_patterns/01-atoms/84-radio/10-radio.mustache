{{# radio }}

  {{# item }}

  <fieldset class="{{ fieldset-class }}">

    <input type="radio" class="{{ radio-class }}" name="{{ name }}" value="{{ value }}" id="{{ id }}" />

    <label class="{{ label-class }}"" for="{{ id }}">
      {{ label-text }}
    </label>

  </fieldset>

  {{/ item }}

</ul>

{{/ radio }}
